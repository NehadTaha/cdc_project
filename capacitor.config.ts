import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cdcmemphremagog.aucoeurdememphre',
  appName: 'Au coeur de Memphré',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
